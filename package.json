{
  "name": "tapitapi",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "core",
    "ui"
  ],
  "scripts": {
    "test": "npm run test --workspace=core",
    "ui": "npm run dev --workspace=ui",
    "build": "npm run build --workspace=ui"
  }
}
